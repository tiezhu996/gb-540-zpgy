# gb-540 地籍边界拓扑冲突消解

请生成一个名为 `cadastral-boundary-topology-resolution` 的 Go 全栈 Web 项目，面向测绘企业和自然资源数据治理团队，用于维护地块边界、导入测量观测、生成边界提案并检测与消解重叠、缝隙和无效拓扑。系统仅做离线决策支持与仿真，不写入法定地籍登记系统、不自动确权或发布法律结论；不得实现商城、电商、预约、订单、仓储库存、财务、客服工单、博客或大众社区。

## 1. 项目概述与复杂度下限

- 类型：B2B 前后端分离全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 四实体必须贯穿 PostgreSQL/PostGIS、Go model/dto/repository/service/handler/router、Vue type/api/store/page；SQLite smoke 使用同一 Go 几何逻辑和 GeoJSON 字段。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；字段、状态、容差、算法版本与权限变化必须触达至少 5 个文件。

## 2. 核心实体

1. `LandParcel`（地块）
   - 字段：`id`、`parcel_code`、`name`、`boundary_geojson`、`coordinate_system`、`area_square_m`、`boundary_version`、`owner_org`、`parcel_state`、`created_at`、`updated_at`。
   - 关系与动作：拥有观测和边界提案，并与相邻地块参与冲突检测；支持导入、几何校验、版本化和归档。
   - 必须贯穿 `land_parcels` 表 -> 独立 model/dto/repository/service/handler/router -> `frontend/src/types/land-parcel.ts`、api、Pinia store 和页面。
2. `SurveyObservation`（测量观测）
   - 字段：`id`、`parcel_id`、`observation_code`、`point_geojson`、`observed_at`、`method`、`horizontal_accuracy_m`、`source_checksum`、`observation_state`、`quality_note`、`imported_by`。
   - 关系与动作：隶属地块并作为提案证据；支持导入、坐标系校验、接受、拒绝和被新观测替代。
   - 必须贯穿 `survey_observations` 表及独立后端分层、前端 type/api/store/page 文件。
3. `BoundaryProposal`（边界提案）
   - 字段：`id`、`parcel_id`、`base_version`、`proposed_geojson`、`observation_ids`、`snap_tolerance_m`、`area_delta_square_m`、`proposal_state`、`rationale`、`version`、`created_by`、`reviewed_by`。
   - 关系与动作：引用基线边界和观测，可能产生多个拓扑冲突；支持草拟、校验、提交、修订、接受或拒绝，但不写入法定系统。
   - 必须贯穿 `boundary_proposals` 表及独立后端分层、前端 type/api/store/page 文件。
4. `TopologyConflict`（拓扑冲突）
   - 字段：`id`、`proposal_id`、`parcel_ids`、`conflict_type`、`geometry_geojson`、`magnitude_square_m`、`severity`、`algorithm_version`、`input_hash`、`conflict_state`、`suggested_resolution_json`、`explanation`、`detected_at`、`resolved_by`。
   - 关系与动作：由提案和相邻地块快照生成；支持检测、确认、标记误报、应用建议到提案新版本和关闭，历史结果不可覆盖。
   - 必须贯穿 `topology_conflicts` 表及独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 所有输入先验证坐标系、环方向、闭合、多边形有效性和自相交；只在约定平面坐标系中按米计算，禁止直接对经纬度做面积/距离。
- 在容差范围内对候选顶点和边做确定性吸附，随后检测相邻地块的 `overlap/gap/self_intersection/dangling_edge`；输出冲突几何、面积/长度、参与地块、吸附前后坐标和建议，不静默修改原边界。
- `BoundaryProposal`：`draft -> validated -> submitted -> reviewed -> accepted/rejected`，`reviewed -> revision -> draft`；接受仅表示内部提案状态，不代表法定确权。非法迁移返回 409。
- `TopologyConflict`：`detected -> confirmed/false_positive -> resolution_proposed -> resolved -> closed`；同一输入哈希、容差和算法版本幂等。
- 几何算法须用成熟库/PostGIS 加固定夹具测试，避免字符串拼接处理 GeoJSON。README 明确法律与精度边界。

## 4. 核心页面与实体消费

1. `/parcels` 地块管理：消费 `LandParcel + SurveyObservation`，展示边界、版本、面积和观测摘要。
2. `/observations` 测量观测：消费 `SurveyObservation + LandParcel`，导入点位、查看精度和坐标系问题。
3. `/proposals` 边界提案：消费 `BoundaryProposal + LandParcel + SurveyObservation`，编辑提案、设置容差和查看版本差异。
4. `/conflicts` 冲突消解：消费 `TopologyConflict + BoundaryProposal + LandParcel`，叠加冲突几何、证据和建议。
5. `/audit` 审计中心：消费四实体变更投影与审计日志，按 request ID、实体、操作者和时间筛选。

`TopologyLegend` 被地块、提案和冲突页共用；`ProposalStateBadge` 被提案和冲突页共用；`GeometryEvidenceDrawer` 被观测、冲突和审计页共用。

## 5. 横切关注点

- JWT + RBAC：角色至少为 `admin`、`surveyor`、`gis_analyst`、`reviewer`、`auditor`；联动角色表、JWT、Go auth/rbac、路由、Vue guard、按钮显隐和类型。提案作者不能评审自己的提案。
- 审计与追踪：所有写操作保存 request ID、操作者、前后几何哈希与结构化差异；算法保存输入哈希、坐标系、容差、版本、耗时和摘要。
- 全局错误：无效几何/坐标系返回 422，状态或基线版本冲突返回 409，权限返回 403，前端统一处理。
- 限流与幂等：登录、观测导入、冲突检测接口分别限流；检测使用 `Idempotency-Key`。

## 6. 共享枚举、组件、hooks 与中间件

- `ConflictType = overlap | gap | self_intersection | dangling_edge`：后端 `backend/internal/constants/conflict_type.go`，前端 `frontend/src/types/enums/conflict-type.ts`。
- `ProposalState = draft | validated | submitted | reviewed | revision | accepted | rejected`：后端 `backend/internal/constants/proposal_state.go`，前端 `frontend/src/types/enums/proposal-state.ts`。
- README 列出枚举在 model、dto、几何算法、service 状态机、store、组件、页面和测试中的所有位置。
- 共享组件：`TopologyLegend`、`ProposalStateBadge`、`GeometryEvidenceDrawer`；hooks/composables：`useAuth`、`useTopologyLayers`。
- 后端至少独立实现 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go`。

## 7. 技术、规模与文件结构

- 英文短名：`cadastral-boundary-topology-resolution`。
- 前端：Vue 3 + TypeScript + Vite + Element Plus + Pinia；后端：Go 1.22 + Gin + GORM；正式 PostgreSQL 16 + PostGIS，runtime smoke 支持 SQLite；JWT。前端使用本地二维 Canvas/几何渲染，不依赖在线地图服务。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行；不计 `_test.go`、生成代码和 vendor。几何校验、吸附、冲突检测、状态机和权限须有测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 纳入 `./backend`，支持根执行 `go work sync`、`go build ./backend/...`、`go test ./backend/...`。

```text
.
├── go.work  runtime_smoke.json
├── docker-compose.yml  .env  .env.example  .gitignore  README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile  cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ geometry/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在全部后端层及前端 api/store/type 中按实体分文件。严禁合并职责到单一文件，不得以通用 CRUD handler/store 替代实体模块。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18540`、后端 `19540`、PostgreSQL 宿主机 `57540`；容器内 `80/8080/5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20540",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20540/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端按 manifest 自动创建 SQLite 表与演示数据，正式 Compose 使用 PostgreSQL/PostGIS；manifest 禁止出现 passed、截图或执行结果字段。
- Compose 顶层 `name: cadastral-boundary-topology-resolution` 且无 `version:`；编排 db/backend/frontend，使用命名卷和英文短名容器名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env`、`.env.example` 均含 `COMPOSE_PROJECT_NAME=cadastral-boundary-topology-resolution`、`FRONTEND_PORT=18540`、`BACKEND_PORT=19540`、`DB_PORT=57540`、`DB_DRIVER=postgres`、DB 变量和 `JWT_SECRET`；`.env` 不提交。前端只访问 `/api`，Nginx 原样代理 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并支持 SPA。
- README 首屏给 `docker compose up -d`，说明项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、坐标与法律边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 必须真实执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck且全部 healthy。至少 8 个真实 API 冒烟覆盖 health、登录、四实体、创建/导入、合法/非法迁移、冲突检测/读取和 403。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 仅使用 Codex 内置 Browser 验证登录、地块、观测、提案、冲突五页，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡。
- 所有构建、测试、runtime smoke、Compose、API、Browser、console/network 与关闭证据只能写 `output/execution.md`；截图直接放 `output/`，至少桌面和移动端各一张并由 execution.md 引用，禁止在其他文件复制证据或伪造通过。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，并停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify cadastral-boundary-topology-resolution"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
